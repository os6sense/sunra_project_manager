<%= simple_form_for(@booking_contact) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :contact_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
